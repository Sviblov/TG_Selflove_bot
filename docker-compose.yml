version: '3.3'

services:
  bot:
    image: "selflove_bot"
    stop_signal: SIGINT
    working_dir: "/usr/src/bot"
    volumes:
      - .:/usr/src/bot
    build:
      context: .
    command: "docker-entrypoint.sh"
    restart: always
    env_file:
      - ".env"
